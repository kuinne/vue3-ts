import foreach from "./foreach";
import map from "./map";
import filter from "./filter";
import find from "./find";
import toArray from "./toArray";

export default {
  foreach,
  map,
  filter,
  find,
  toArray,
};

export { foreach, map, filter, find, toArray };
